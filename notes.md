Done:

- Create data dir and move df there
- Create notebooks dir and move jupyters there
- Change notebooks path to read data from the correct directory
- Create `schemas.py`, the `pydantic` request/response models
- Create script that merges all the work to create the API model and metadata
- Create UI
- Create test examples

To Do:

- Add chunk that writes cleaned df to correct place
- Group all imports on a top chunk
- Separate notebooks per collaborator

- in README: add repository structure

- Create API README
- Deploy API
